# Changelog

All notable changes to this project are documented here. Entries are appended
automatically by [`.github/workflows/release.yml`](.github/workflows/release.yml)
when a `vX.Y.Z` tag is pushed — each section below is GitHub's generated
release notes (PRs merged since the previous tag) for that version, committed
straight to `main`. Don't hand-edit past entries; fix the source PR titles
instead, the format regenerates from those.

## [Unreleased]
